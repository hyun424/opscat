# P131-001 Runtime Contract

Define strict, hash-bound configuration for a standalone read-only monitoring
process. Reject credentials, mutation capabilities, unsafe paths, unsupported
sources, and invalid cadence/freshness relationships.

