# P33-003 — Mock transport probe: simulate latency, timeout, rate-limit, retry, and health states without live API calls.

## Goal

Implement this P33 slice while preserving dry-run/read-only behavior.

## Verification

- P33 targeted tests pass.
- Lint/type checks pass for changed files.
- Full verification profile passes before evidence is finalized.
