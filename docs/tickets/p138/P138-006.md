# P138-006 - Frozen release evidence and verification profile

## Scope

Write failing structural-evidence/profile tests, then implement source-bound
P138 release validation and wire `p138-release` only after the exact 30-case
runner and targeted tests are stable.

## Acceptance

- Release evidence independently rebuilds 30/30 totals, phase paths, component
  calls, durable post-state, exact authority/runtime/evaluator maps, resources,
  and exact-zero shared forbidden authority from case evidence.
- The freeze manifest and final review bind the complete transitive P136
  two-phase cycle-outcome implementation, publisher lease/recovery, P137, and
  P138 runtime/contracts/validators, runner, fixtures, profile, matrix, cases,
  tests, and docs.
- Preliminary and final use exactly
  `evals/p138/output/canonical-matrix.json`,
  `evals/p138/output/freeze-manifest.json`,
  `evals/p138/output/release-evidence.json`, and
  `evals/p138/final-implementation-review.json`.
- Final mode and `p138-release` regenerate no reviewed input and reject any
  source/profile/fixture/case/matrix/review hash drift, missing final review,
  unresolved P0/P1/P2 finding, counter-key mismatch, nonzero authority,
  resource overflow, or total other than 30/30.
- P138 binds P136/P137 qualified statuses and hashes but does not re-claim their
  denominators; their release profiles run separately.
- `p138-release` runs targeted tests, Ruff, Mypy, exact matrix/evidence checks,
  and static authority/leak checks against the tracked paths.
- `UV_CACHE_DIR=/tmp/opscat-uv-cache bash scripts/verify.sh --profile p138-release`
  passes before P138-007 starts.
