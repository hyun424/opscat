# P138-007 - Documentation and operator boundary

## Scope

Refresh P138 roadmap, test spec, tickets, and release-facing documentation so
the amended reconciliation, commit-boundary recovery, 30-case evidence, and
local no-authority claim are exact and reproducible.

## Acceptance

- Documentation is reconciliation-first and contains no superseded runner,
  linear component-sequencing, or non-30-case release language.
- Docs state the exact six phases and empty-delta path, P136 outcome intent ->
  checkpoint -> completion protocol and recovery tuples, publisher
  whole-operation lease and all split-commit recovery tuples, genesis-only
  bootstrap, contiguous delta, sole lock order, exact counters, and all 30 case
  meanings.
- Docs preserve the explicit no authentication, provider/live-connector,
  network/DNS/socket, credential/environment, subprocess/shell, runtime signal,
  notification/delivery, action/remediation, staging/production mutation, or
  operator-replacement authority boundary.
- P138 is described only as a production-shaped local finite supervisor over
  P136/P137, never unattended production operation.
- Preliminary/final commands and artifacts use the matching tracked
  `evals/p138/output` paths and external
  `evals/p138/final-implementation-review.json`; final mode regenerates no
  reviewed input.
- Ticket README preserves dependency order, failing-test gates, 30/30 release
  gate, separate P136/P137 profiles, and exact qualified status.
- Documentation and docs-profile checks pass before P138-008 starts.
