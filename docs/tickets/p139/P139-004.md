# P139-004 — Hardened deployment contracts

Add and validate exact systemd and Compose definitions with no network, no new
privileges, immutable image identity, read-only root, explicit mounts, bounded
restart behavior, non-root identity, and safe SIGTERM. Keep launchd explicitly
unqualified.
