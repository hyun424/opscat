# P114-011: service-agnostic fault KNN

Train a service-name-free robust 3-NN fault classifier from RE2-SS metric
signatures. Require leave-one-root-service-out development gates before it can
be frozen for RE2-OB. The artifact must contain no service names, case IDs,
commands, actions, or authority.
