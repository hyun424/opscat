# P132-003 Supervisor Contracts

Status: complete.

Provide structurally validated systemd, launchd, and Docker Compose examples.
Require the closed monitor command, restart throttling, graceful stop, bounded
timeouts, local paths, and no shell, embedded credentials, privileged mode,
host networking, live connector, or remediation command.
