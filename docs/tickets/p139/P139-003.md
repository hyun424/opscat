# P139-003 — Service API and CLI

Implement validate/run/status APIs and the `opscat-triage-service` entrypoint.
Use explicit bundle/base paths only, safe signal handling, one canonical JSON
result, fixed exit codes, read-only status, and no environment fallback.
