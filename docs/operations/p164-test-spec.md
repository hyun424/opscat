# P164 Test Spec

- Start a real numeric-loopback provider-shaped telemetry server.
- Read metrics, logs, traces, deploy metadata, topology, and health.
- Observe healthy, precursor, and incident stages for all six fault families.
- Verify secrets are redacted and source shapes remain deterministic.
- Reject DNS names, redirects, non-loopback URLs, and unknown endpoints.
- Produce predecessor- and source-bound evidence with all external authority
  counters exactly zero.
