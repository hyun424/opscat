# P113-000: failure decomposition and governance

Record P112 as consumed evidence, separate deterministic-model and provider
contract failures, and declare Train Ticket as the next untouched blind system.
