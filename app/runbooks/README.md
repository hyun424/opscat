# OpsCat local runbooks

Runbooks are registered in `app/services/runbook_service.py` while P6 remains local/mock. Each step declares preconditions, permission, risk hint, dry-run support, rollback expectation, and verification check.
