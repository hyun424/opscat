# P112-002: service-agnostic diagnostic features

Build deterministic local, rank, contrast, share, and coupling features for
each visible service/fault hypothesis without service-name or label leakage.
