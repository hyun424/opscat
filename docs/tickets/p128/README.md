# P128 tickets

P128 defines the operator evidence/replay UX planning phase. Implementation is
pending.

1. `[planned] P128-001` - replay and evidence navigation model
2. `[planned] P128-002` - quality, resilience, and fail-closed views
3. `[planned] P128-003` - receipt provenance, counters, and limitations
4. `[planned] P128-004` - read-only UX guardrails and redaction
5. `[planned] P128-005` - verification handoff and beta dependency gate

Auth is deferred, credentials are out of scope, production/staging mutation is
forbidden, and authority counters remain exactly zero.

See `docs/operations/p128-operator-evidence-replay-ux-roadmap.md`,
`docs/operations/p128-test-spec.md`,
`docs/operations/p128-plan-review.md`, and
`docs/operations/p128-verification-handoff.md`.

Schema marker: `p128.ticket_index.v1`.

